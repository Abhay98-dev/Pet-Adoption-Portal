<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pet.name %> - Pet Adoption Portal</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    .pet-detail-container {
      display: flex;
      align-items: flex-start;
      gap: 20px;
      padding: 40px;
      max-width: 900px;
      margin: 0 auto;
    }

    .pet-detail-container img {
      width: 300px;
      height: auto;
      border-radius: 12px;
      object-fit: cover;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }

    .pet-info {
      flex: 1;
    }

    .pet-info h2 {
      margin-top: 0;
      font-size: 2rem;
      color: #444;
    }

    .pet-info p {
      margin: 8px 0;
      font-size: 1.1rem;
    }

    .btn {
      display: inline-block;
      padding: 10px 20px;
      margin-top: 15px;
      background: #ff6f91;
      color: white;
      border-radius: 8px;
      text-decoration: none;
      font-weight: bold;
      transition: 0.3s;
    }

    .btn:hover {
      background: #ff4f70;
    }

    /* Header and footer styling */
    header {
      background: #ffc0cb;
      padding: 15px 30px;
    }
    header nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    header ul {
      list-style: none;
      display: flex;
      gap: 20px;
      margin: 0;
      padding: 0;
    }
    header ul li a {
      text-decoration: none;
      color: white;
      font-weight: bold;
    }
    footer {
      background: #f8f8f8;
      text-align: center;
      padding: 15px;
      margin-top: 40px;
      border-top: 1px solid #ddd;
    }
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <nav>
      <h1>🐾 Pet Adoption Portal</h1>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/pet">Pets</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <!-- Pet Details -->
  <div class="pet-detail-container">
    <img src="<%= pet.image %>" alt="<%= pet.name %>">

    <div class="pet-info">
      <h2><%= pet.name %></h2>
      <p><strong>Type:</strong> <%= pet.type %></p>
      <p><strong>Breed:</strong> <%= pet.breed %></p>
      <p><%= pet.description %></p>
      <form action="/adoption/<%= pet._id %>" method="POST">
  <button type="submit" class="btn">Adopt <%= pet.name %></button>
</form>

    </div>
  </div>

  <!-- Footer -->
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Pet Adoption Portal. All rights reserved.</p>
  </footer>
</body>
</html>
