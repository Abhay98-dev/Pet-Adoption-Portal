<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pet Adoption Portal</title>
  <link rel="stylesheet" href="/style.css"> 
</head>
<body>
  <!-- Header -->
  <header>
    <nav>
      <h1>🐾 Pet Adoption Portal</h1>
      <ul>
        <li><a href="/about">About</a></li>
        <li><a href="/contact">Contact</a></li>
      </ul>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <h2>Find your new best friend today! 🐶🐱</h2>
    <p>Adopt, don’t shop. Give a loving home to pets in need.</p>
    <a href="/pet" class="btn">View Available Pets</a>
  </section>

  <!-- Featured Pets -->
  <section class="pets">
    <h2>New Featured Pets</h2>
    <div class="pet-list">
    <% if (pets && pets.length > 0) { %>
  <% pets.slice(0, 4).forEach(pet => { %>
    <div class="pet-card">
      <img src="<%= pet.image %>" alt="<%= pet.name %>">
      <h3><%= pet.name %></h3>
      <p>Breed: <%= pet.breed %></p>
      <p><%= pet.description %></p>
      <a href="/pet/<%= pet._id %>" class="btn">Adopt Me</a>
    </div>
  <% }) %>
<% } else { %>
  <p>No pets available right now. Please check back later!</p>
<% } %>

    </div>
  </section>

  <!-- Footer -->
  <footer>
    <p>&copy; <%= new Date().getFullYear() %> Pet Adoption Portal. All rights reserved.</p>
  </footer>
</body>
</html>
